[server]
# Gadget mode is the supported default. Pass --server explicitly to the Zero
# installer only when intentionally using the Wi-Fi escape hatch.
address = "10.99.0.1"
connect_timeout_seconds = 3
read_timeout_seconds = 5

[agent]
poll_interval_seconds = 30
state_directory = "/var/lib/nas-display"
